# Vending Machine

This project implements a **Vending Machine Controller** using a **Moore Finite State Machine (FSM)** in SystemVerilog. The vending machine accepts **Rs. 5** and **Rs. 10** coins and dispenses a product when the total amount reaches **Rs. 15**. The design tracks the inserted amount through different states and returns to the initial state after dispensing the product.
